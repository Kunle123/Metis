# Exact chrome-reduction validation notes

## Dashboard
- The top dashboard strip now reads **Operational snapshot** in a lighter, smaller treatment.
- The right-hand secondary cards use lighter **Templates** and **Updates** headings rather than the earlier double-title treatment.
- No new duplicate dashboard title block or extra helper chrome is visible in the reviewed viewport.

## Pending review
- Brief, Setup, Compare, Gaps, Input, Export, and Sources still need focused browser review in this pass.

## Brief
The brief artifact no longer repeats the issue title inside the document header. The top of the artifact now opens directly with the summary sentence and a quieter metadata strip, while the right rail is compressed into fewer support modules. Section numbering appears reduced in visual weight, and the appendix heading is lighter than the body section headings.

## Export
The export rail now shows a distinct **Preview** heading, and **Package contents** reads as a lighter support heading rather than a competing section title. The circulation checks summary remains folded into the checks panel rather than breaking out into a separate summary module.

## Input
The input screen keeps the requested content while reducing chrome: the **Current record** label is visually quieter, and the right rail now reads as one lighter support panel with the effect state folded into it.

## Compare
The compare screen no longer shows the extra page-meta label. The version context above the compare surface is lighter, and the lower readiness block now reads as a quieter **Circulation state** section rather than a heavier standalone panel heading.

## Setup
The setup screen now presents a single main **Intake record** heading beneath the page title, with no extra page-meta label above the shell title. The reviewed viewport shows the requested reduced heading stack.

## Gaps
The gaps screen continues to use a single kept main title, **Clarification gaps**, without an additional page-meta label. The support area remains headed by one compact **By section** grouping and does not introduce an extra framing title.

## Sources
The sources page still shows the tightened one-line summaries and the lighter heading stack, with the count strip carrying the top of the ledger instead of a repeated section heading. The right rail remains compact and the simplified metadata treatment renders cleanly.
